# Claude Code Container Configuration

# GitHub token for git/gh operations
GITHUB_TOKEN=

# Anthropic API key file path
ANTHROPIC_KEY_FILE=~/.anthropic_key

# Project directory to mount
PROJECT_DIR=.

# Branch protection - branches that cannot be pushed to
BLOCKED_BRANCHES=["main","master"]

# Allowed branch patterns (if set, only these patterns allowed)
# Example: ["claude/*","feature/*"]
ALLOWED_BRANCH_PATTERNS=[]

# Debug logging
AGENT_DEBUG=false
